# Settings for dotfiles.py

decrypted_dir: decrypted
encrypted_tar: encrypted.tar.gpg
template_dirs:
  - templates
  - decrypted
out_dir: generated
verify_script_filename: .dotfiles_verify.sh
setup_env_script_filename: .dotfiles_setup.sh
